export component App inherits Window {
    title: "rs-checkers";

    Rectangle {
        background: #1b1f23;

        VerticalLayout {
            padding: 16px;
            spacing: 12px;

            Text {
                text: "rs-checkers";
                font-size: 22px;
                color: #ffffff;
                horizontal-alignment: center;
            }

            Rectangle {
                width: 400px;
                height: 400px;
                border-color: #9aa0a6;
                border-width: 3px;
            }
        }
    }
}
